# Rename to secrets.toml for local runs, or paste into
# Streamlit Cloud -> App -> Settings -> Secrets.
ANTHROPIC_API_KEY = "sk-ant-..."
APP_PASSWORD = "pick-a-code-to-share"   # optional; remove to make the demo open
MAX_QUERIES = "25"
